<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.DeviceUserSyncMapper">

	<select id="scan" resultType="com.example.demo.model.DeviceUserSync">
		SELECT
		*
		FROM
		t_device_user_sync
		limit #{limit}
	</select>
</mapper>